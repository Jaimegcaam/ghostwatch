# Template — DO NOT commit your real secrets to git.
# Create the real secret with:
#
#   kubectl -n ghostwatch create secret generic ghostwatch-secrets \
#     --from-literal=AUTH_SECRET=$(openssl rand -base64 32) \
#     --from-literal=CRON_SECRET=$(openssl rand -base64 32) \
#     --from-literal=POSTGRES_PASSWORD=$(openssl rand -base64 24) \
#     --from-literal=RESEND_API_KEY=re_xxx \
#     --from-literal=GOOGLE_CLIENT_ID= \
#     --from-literal=GOOGLE_CLIENT_SECRET= \
#     --from-literal=GITHUB_ID= \
#     --from-literal=GITHUB_SECRET=
#
# Then DELETE this file from your tree, or keep it as a placeholder.
apiVersion: v1
kind: Secret
metadata:
  name: ghostwatch-secrets
  namespace: ghostwatch
type: Opaque
stringData:
  AUTH_SECRET: "replace-me"
  CRON_SECRET: "replace-me"
  POSTGRES_PASSWORD: "replace-me"
  RESEND_API_KEY: ""
  GOOGLE_CLIENT_ID: ""
  GOOGLE_CLIENT_SECRET: ""
  GITHUB_ID: ""
  GITHUB_SECRET: ""
